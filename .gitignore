# Demo target, cloned locally at the pinned commit in configs/demo.yaml.
# Never committed - it is someone else's repo and it would swamp every diff.
/intervals-mcp-server/

# Python
.venv/
.venv-ci/
venv/
.flow_cache/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Env + secrets. Secrets never enter the sandbox and never enter git.
.env
.env.*
!.env.example
.env.local

# Node (agents/seed.ts)
node_modules/

# Local state
*.sqlite
*.db
logs/
*.log
uv.lock
.DS_Store
